# vizdoom-turbo migration package

`vizdoom-turbo` has moved to
[`env-vizdoom-turbo`](https://pypi.org/project/env-vizdoom-turbo/).

This final metadata-only release installs the replacement distribution at the
same version. Existing `vizdoom_turbo` imports remain unchanged.
